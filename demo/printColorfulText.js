const { print } = require('../dist'); // const { print } = require('terminal-parcel');

print('<color apply="green">outputs green text</color>');
print('<color apply="red.underline">outputs red underlined text</color>');
print('<color apply="inverse">inverse the color</color>');
print('<color apply="rainbow">OMG Rainbows!</color>');
print('<color apply="trap">Drops the bass</color>');
